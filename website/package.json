{
  "name": "quantyx-pricer-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "mkdir -p public && cp -f ../output/prices.json public/prices.json || true",
    "dev": "vite",
    "local": "VITE_API_URL=http://127.0.0.1:8000 vite",
    "prod": "VITE_API_URL=https://lux-pricer-eta2cxamh7evctdv.switzerlandnorth-01.azurewebsites.net vite --mode production",
    "build": "vite build",
    "build:local": "VITE_API_URL=http://127.0.0.1:8000 vite build --mode development",
    "build:prod": "VITE_API_URL=https://lux-pricer-eta2cxamh7evctdv.switzerlandnorth-01.azurewebsites.net vite build --mode production",
    "preview": "vite preview",
    "start": "vite",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^4.4.0",
    "sass": "^1.70.0"
  }
}
