{
  "name": "quantyx-pricer-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "mkdir -p public && cp -f ../output/prices.json public/prices.json || true",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^4.4.0",
    "sass": "^1.70.0"
  }
}
